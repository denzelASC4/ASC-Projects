var sums = [2, 4, 7, 13, 52, 76, 12, 9, 61, 11];

function addNum(sums) {
    x = 0;
    for (var i = 0; i < 10; i++) {
        x = x + sums[i];


    }
}







console.log(sums.length);
a = 0
for (var i = 0; i < sums.length; i++) {
    a = a + sums[i]
}
console.log(sums[i]);


function rand(n) {
    sumArray = [];
    var addNumber = Math.floor(Math.random() * n);

    ///////


    var myArray = [];
    for (var i = 0; i < 10; i++) {
        myArray[i] = Math.floor9Math.random() * 10);
        myArray.push(num);
    }
    function findSum(myArray) {
        var a = 0;
        for (var i = 0; i < 10; i++) {
            a = a + myArray[i];
        }
        return a;
    }



    var car = {
        color: "red",
        name: "Audi",
        year: "3005",
        getName: function () {
            return car.name;
        },
        getColor: function () {
            return car.color;
        }
    };
    function Car(color, name, year) {
        this.color = color;
        this.name = name;
        this.model = year;
    this.getName = function(newColor)
    this.color- newColor
}

